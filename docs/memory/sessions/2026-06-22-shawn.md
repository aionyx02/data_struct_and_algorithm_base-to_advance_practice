---
type: session_log
status: archive
priority: p3
updated: 2026-06-22
context_policy: historical
owner: shawn
---

# Session Log: 2026-06-22 - Shawn

## Summary

- Committed staged TASK.WEB.002 (resizable panes + loopback compile check) to
  main as `dae3201`, excluding the unrelated `演算法/*.pdf` changes.
- Planned a competitive-programming curriculum expansion (algorithms to
  WF/IOI level + a full number-theory/math track), captured as a proposed ADR
  plus two roadmap docs.

## Actions Taken

- `git commit dae3201`: TASK.WEB.002 web/scripts/test/docs paths only.
- Created `docs/adr/0006-...` (proposed) recording the DS-only scope expansion
  that the developer explicitly requested; designed roadmaps "toward future
  Judge problems" but gated implementation on ADR acceptance.
- Extended `docs/algorithm-roadmap.md`: added L6 difficulty, an International
  tier G9–G15 (advanced graphs/tree-divide, DP+convex opt, suffix-automaton
  strings, geometry, randomization/heuristics, construction/interactive,
  capstone), a WF/IOI acceptance section, and 🧪 Judge-candidate notes per stage.
- Created `docs/math-roadmap.md`: M0–M12 number-theory + math track
  (mod arithmetic → sieves/Min_25 → combinatorics → generating functions →
  linear algebra → polynomial suite → probability → group counting → games →
  transforms/numerics → analytic NT), each with acceptance + Judge-candidate.
- Wired routing: index Intent Routing row + auto-regenerated Document Map and
  decision index; added a gated backlog section under ADR-0006.

## Commands / Validation

```powershell
npm test            # 16/16 pass
npm run docs:ready  # all guards green (schema, links, adr-status, task-status,
                    # frontmatter, narrative routing); docs ready ok
```

Result: pass.

## Detailed Notes

- The inherited compaction summary was stale: it described mid-flight A37 work,
  but git log shows Stages A5–A8 (incl. A37) already implemented and committed.
  Reoriented to the actual working tree before acting.
- project.md non-goals intentionally left unchanged: ADR-0006 is `proposed`, so
  the DS-only Judge boundary still holds until the developer accepts it.
- Geometry/construction/interactive/heuristic topics are flagged as needing
  their own follow-up implementation ADRs (special judge, float tolerance,
  interactive runner) before they can become Judge problems.

## ADR-0006 accepted + first M-series Judge slice

- Developer accepted ADR-0006; flipped status to `accepted` and lifted the
  DS-only gate in `project.md` non-goals + Current Strategy and `current.md`.
- Started `TASK.CURRICULUM.020` (M-series number theory). Implemented
  `M01-modular-arithmetic-toolkit` end-to-end following the proven A-series
  pattern:
  - `problems/math/number-theory/M01-.../` problem.json (stage `M`, D2),
    statement, 4 tests with reference-generated `.out`.
  - operations: `gcd`, `pow` (fast exp + 128-bit modmul), `inv` (extended
    Euclid, `NONE` when not coprime), `lcm` mod m. forbidden: std::gcd/lcm/
    __gcd/pow + `<numeric>` (force hand implementation).
  - fixtures: correct + two known-wrong — `fermat_inverse` (a^(m-2), wrong for
    composite m / never NONE) and `lcm_premod` (reduces operands before gcd/lcm).
  - `src/generators/number_theory_generators.cpp`: embedded oracle + pinned
    prefix that forces both bugs to diverge every stress case; wired into
    registry, assembly chain, CMake (source + catalog + 3 judge + 3 stress).
- Fixed a latent build flake: `progress_repository_test` linked libstdc++
  dynamically and failed under CTest with 0xc0000139 (entry-point-not-found).
  Applied the same `-static -static-libgcc -static-libstdc++` the project
  already uses for `algo`. Now passes.

## Commands / Validation (curriculum slice)

```powershell
cmake --build --preset dev
ctest --preset dev   # 750/750 passed (~490 s)
algo list --root .   # 133 problems; M01 listed as stage M / D2
```

## Stage M1 completion (M02-M06)

- Completed stage M1 number theory by adding five more problems, each end-to-end
  (problem package + correct + 2 known-wrong fixtures + differential generator +
  CMake catalog/judge/stress):
  - M02 linear-congruence-solver: `solve` a*x=b (mod m) with exact solution
    count, general `crt` for non-coprime moduli. Wrongs: count-one, coprime-only
    CRT.
  - M03 prime-sieve-queries: linear sieve to 1e6; is_prime/count/nth/spf.
    Wrongs: 1-is-prime, count excludes endpoint.
  - M04 divisor-functions: trial-division factorize + d/sigma/phi to 1e12.
    Wrongs: divisor count missing +1, phi dropping p^(e-1).
  - M05 big-factorization: deterministic Miller-Rabin + Pollard's Rho to 1e18;
    is_prime/max_factor/num_factors/factor_count. Wrongs: base-2-only MR,
    distinct-vs-multiplicity.
  - M06 modular-binomial: linear inverse + factorial/inverse-factorial tables
    mod 1e9+7; inv/fact/inv_fact/binom. Wrongs: inverse sign error, C(n,n)=0.
- All references verified against hand-computed values before generating `.out`;
  every wrong fixture confirmed to diverge on the fixed tests.
- Generators embed each oracle with a pinned prefix forcing both bugs to diverge
  every stress case (e.g. is_prime 2047 for the MR base bug, binom 5 5 for the
  off-by-one boundary).

## Commands / Validation (stage M1)

```powershell
cmake --build --preset dev
ctest --preset dev   # 785/785 passed (~476 s)
algo list --root .   # 138 problems; M01-M06 listed as stage M
```

Result: pass.

## Stage M2 (M07-M09)

- Stage M1 committed as `fc44bf5`. Continued with stage M2 congruence theory,
  scoped to prime moduli for reliable exact-match differential testing:
  - M07 discrete-logarithm: BSGS `log a b p`. Wrongs: 0-for-b=0, dropped x=0
    fast path (returns the order instead of 0).
  - M08 primitive-root: `order`/`primitive_root`/`num_primitive_roots`/
    `is_primitive` via factoring p-1. Wrongs: order assumes primitive,
    is_primitive checks only Fermat (a^(p-1)=1 holds for all a).
  - M09 quadratic-residue: `legendre`/`is_qr`/`sqrt` (Tonelli-Shanks, both
    p%4==3 and p%4==1 paths). Wrongs: Legendre drops a=0, sqrt returns the
    larger root.
- All references hand-verified before generating `.out`; every wrong confirmed
  to diverge. Generators embed each oracle with a pinned prefix.
- Build: 141 problems; M07-M09 catalog/judge/stress all green (21/21).
- Noted a pre-existing flake: judge_accepts_link_cut_tree (A54) can TLE under
  parallel CTest load but passes in isolation (1.16 s); untouched by M-series.

## Completed Markers

- TASK.WEB.002 completion is recorded in `2026-06-21-shawn.md` (committed as
  `dae3201` this session). TASK.CURRICULUM.020 remains in progress: stages M1
  (M01-M06) and M2 (M07-M09) are complete and green, but the broader
  math/algorithm curriculum continues, so no COMPLETED marker yet.
