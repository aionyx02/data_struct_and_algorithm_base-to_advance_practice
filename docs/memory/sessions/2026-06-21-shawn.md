---
type: session_log
status: archive
priority: p3
updated: 2026-06-21
context_policy: historical
owner: shawn
---

# Session Log: 2026-06-21 - Shawn

## Summary

- Built Stage A4 Union-Find Variants in full: `A29`-`A35` (7 problems).
- New advanced family directory `problems/advanced/union-find/` and a new
  generator domain `src/generators/union_find_generators.cpp`.
- Each problem ships problem metadata, statement, four fixed tests, one correct
  fixture, two known-wrong fixtures, and a seeded differential generator with a
  pinned invariant prefix.
- Full C++ validation passed: 538/538 CTest integration tests; `algo list`
  enumerates 103 problems (`F01`-`F68`, `A01`-`A35`).

## Actions Taken

- Added seven problem packages under `problems/advanced/union-find/`:
  - `A29-union-by-rank-path-compression` (D2): union by rank + full path
    compression; `rank`, `find`, `parent`, `connected`, `components`,
    `snapshot`. Distinct from `F45` (size) and `F46` (trace).
  - `A30-dsu-amortized-cost` (D2): `find` prints pre-compression edge count;
    cost falls after compression.
  - `A31-potential-weighted-dsu` (D3): `relate a b w` / `diff` over integer
    potentials with `CONSISTENT`/`CONFLICT`/`UNKNOWN`.
  - `A32-parity-dsu` (D3): two-coloring with `relate same|diff` / `query`.
  - `A33-rollback-dsu` (D3): union by size, no compression, manual undo log with
    `checkpoint`/`rollback`.
  - `A34-persistent-dsu` (D4): versioned `unite v a b` forking new versions;
    queries target any past version.
  - `A35-successor-dsu` (D3): next-free-slot DSU with `assign`/`next`/`used`/
    `remaining`.
- Added `src/generators/union_find_generators.cpp` with one embedded-oracle
  generator per problem and `registerUnionFindGenerators`; declared it in
  `src/generators/generator_registry.hpp` and chained it in
  `src/case_generator.cpp`.
- Registered `union_find_generators.cpp` in the `judge_core` sources and added
  7 catalog tests, 21 deterministic Judge tests, and 21 stress tests in
  `CMakeLists.txt`.
- Added 21 submission fixtures under `tests/fixtures/submissions/` (one correct
  and two known-wrong per problem).
- Generated every fixed-test `.out` by running the compiled correct fixture on
  the hand-crafted `.in`, never by hand.

## Commands / Validation

```bash
cmake --preset dev
cmake --build --preset dev
ctest --preset dev
build/mingw-debug/algo.exe list --root .
```

Result: pass. Full CTest run passed 538/538 tests in ~250 s (489 prior + 49
new: 7 catalog, 21 Judge, 21 stress). `algo list` enumerates 103 problems
(`F01`-`F68`, `A01`-`A35`). A 100-case differential sweep (seed 99) confirmed
all 7 correct fixtures verdict `AC` and all 14 known-wrong fixtures verdict
`WA`.

## Detailed Notes

- One off-by-one bug was found and fixed during stress validation: the `emitted`
  counters for the A29 and A33 generators started one above the actual pinned-op
  count, so generated inputs declared one more operation than they contained,
  causing the correct fixtures to fail stress. Fixed to 12 (A29) and 10 (A33).
- Each generator pins an invariant prefix that buries a populated/weighted root
  (A29/A31/A32), forces a depth-2 find (A30), runs a multi-union rollback past a
  compressing query (A33), forks old versions before querying them (A34), or
  re-assigns the same start slot (A35), so the matching known-wrong fixtures
  diverge deterministically rather than relying on random luck.
- `A33` adds `std::stack` to `forbidden_symbols` so the undo log must be
  hand-managed; this is the rollback-vs-compression lesson.
- `A34` uses copy-on-write full arrays for the oracle and reference (correct for
  small inputs); the curriculum point about structural sharing is left to the
  solver, with `memory_limit_mb` raised to 128.

## Completed Markers

## COMPLETED: TASK.CURRICULUM.018 - Build Union-Find Variants

Added all seven Stage A4 Union-Find Variants exercises (`A29`-`A35`) with
problem packages, statements, fixed tests, differential generators, correct
fixtures, and known-wrong fixtures, plus the new `union-find` family directory
and `union_find_generators.cpp` domain. Final validation passed 538/538 CTest
integration tests, and `algo list` enumerates 103 problems (`F01`-`F68` and
`A01`-`A35`).

## Stage A5 Follow-up Summary

- Completed all nine Balanced/Randomized/Meldable Tree problems, `A36`-`A44`.
- Added four fixed tests, one correct fixture, two focused known-wrong fixtures,
  and a seeded differential generator for every problem.
- Registered 9 catalog tests, 27 deterministic Judge tests, and 27 stress tests.
- Full C++ validation passed 601/601 tests; `algo list` now enumerates 112
  problems (`F01`-`F68`, `A01`-`A44`).

## Stage A5 Actions

- Added `problems/advanced/balanced-tree/` packages for:
  - `A36-order-statistic-avl`: unique-key AVL with subtree sizes.
  - `A37-order-statistic-red-black`: full red-black insertion/deletion plus
    rank, select, predecessor, and successor.
  - `A38-treap-split-merge`: explicit-priority max-heap Treap.
  - `A39-implicit-treap-sequence`: index split/merge, lazy reversal, range sum.
  - `A40-splay-tree-access`: zig/zig-zig/zig-zag access and subtree sizes.
  - `A41-leftist-heap-meld` and `A42-skew-heap-meld`: multi-heap ownership and
    destructive meld.
  - `A43-binomial-heap`: degree consolidation, meld, and extract-min.
  - `A44-order-statistic-multiset`: duplicate counts, `kth`, and
    `order_of_key` semantics.
- Added `src/generators/balanced_tree_generators.cpp`, registered it with the
  generator registry, and added the complete CMake integration matrix.
- Kept the shared submission-fixture implementation in
  `balanced_tree_fixture.hpp`; wrappers select the exercise and deliberate bug,
  so the invariant differences remain explicit without duplicating thousands
  of lines.
- Fixed a Splay insertion defect found while producing fixed outputs: insertion
  after a failed search now splits around the splayed root instead of replacing
  one of its existing child subtrees.
- Changed the Skew/Binomial source-alias wrong fixtures to retain safe copies,
  producing deterministic `WA` rather than use-after-free `RE`.
- Strengthened the Binomial stress prefix to expose reversed root-degree order
  before random operations.

## Stage A5 Validation

```bash
cmake --preset dev
cmake --build --preset dev
ctest --preset dev
build/mingw-debug/algo.exe list --root .
```

Result: 601/601 CTest tests passed in about 349 seconds. The Stage A5 targeted
matrix passed 63/63. A 100-case differential sweep at 120 operations per case
(seed 99) returned `AC` for all 9 correct fixtures and `WA` for all 18
known-wrong fixtures.

## COMPLETED: TASK.CURRICULUM.019 - Build Balanced, Randomized, And Meldable Trees

Added all nine Stage A5 exercises (`A36`-`A44`) with problem packages,
statements, fixed tests, differential generators, correct fixtures, and focused
known-wrong fixtures. Full validation passed 601/601 tests, and the catalog now
contains 112 problems (`F01`-`F68` and `A01`-`A44`).

## Stage A6 Follow-up Summary

- Completed all ten Tree Query Structure problems, `A45`-`A54`.
- Added four fixed tests, one correct fixture, two focused known-wrong fixtures,
  and a seeded differential generator for every problem.
- Registered 10 catalog tests, 30 deterministic Judge tests, and 30 stress
  tests. The full CTest matrix now contains 671 tests.
- `algo list` now enumerates 122 problems (`F01`-`F68`, `A01`-`A54`).

## Stage A6 Actions

- Added `problems/advanced/tree-query/` packages for Euler Tour intervals,
  Binary Lifting, LCA/path queries, deterministic HLD layout, HLD with path and
  subtree aggregation, Small-to-Large merging, DSU on Tree, Centroid
  Decomposition, Virtual Tree construction, and dynamic Link-Cut Tree behavior.
- Added `src/generators/tree_query_generators.cpp` with a shared static-tree
  oracle, seeded query/update generation, pinned structural invariants, and a
  brute dynamic-forest oracle for Link-Cut Tree operations.
- Added `tree_query_fixture.hpp` plus 30 small wrappers. The fixture modes cover
  deterministic traversal and tie-break contracts while the bug modes isolate
  interval, endpoint, merge, ownership, decomposition, and dynamic-forest
  errors.
- Registered the generator domain and the complete Stage A6 catalog/Judge/stress
  matrix in the existing decoupled generator registry and CMake test harness.

## Stage A6 Validation

```bash
cmake --preset dev
cmake --build --preset dev
ctest --preset dev
build/mingw-debug/algo.exe list --root .
```

Result: 671/671 CTest tests passed in about 415 seconds. The Stage A6 targeted
matrix passed 70/70. A 100-case differential sweep at 120 operations per case
(seed 99) returned `AC` for all 10 correct fixtures and `WA` for all 20
known-wrong fixtures.

## COMPLETED: TASK.CURRICULUM.020 - Build Tree Query Structures

Added all ten Stage A6 exercises (`A45`-`A54`) with problem packages,
statements, fixed tests, differential generators, correct fixtures, and focused
known-wrong fixtures. Full validation passed 671/671 tests, and the catalog now
contains 122 problems (`F01`-`F68` and `A01`-`A54`).

## Stage A7 Follow-up Summary

- Completed all six String Index Structure problems, `A55`-`A60`.
- Added four fixed tests, one correct fixture, two known-wrong fixtures, and a
  seeded differential generator for every problem.
- Added Trie multiset, compressed Radix dictionary, XOR Trie multiset,
  Aho-Corasick overlap matching, Suffix Array/LCP queries, and Suffix Automaton
  indexing packages under `problems/advanced/string-index/`.
- Registered 6 catalog, 18 deterministic Judge, and 18 stress tests. The full
  matrix now contains 713 tests and the catalog contains 128 problems.
- Strengthened the fixed Suffix Array suite with a non-adjacent rank interval so
  an incorrect single-adjacent-LCP implementation cannot pass by coincidence.

## Stage A7 Validation

`cmake --preset dev`, `cmake --build --preset dev`, and `ctest --preset dev`
passed. Full result: 713/713 tests in about 431 seconds. A 100-case differential
sweep (seed 99, 120 operations per case) returned `AC` for all 6 correct
fixtures and `WA` for all 12 known-wrong fixtures.

## COMPLETED: TASK.CURRICULUM.021 - Build String Index Structures

Added all six Stage A7 exercises (`A55`-`A60`) with problem packages, fixed
tests, differential generators, correct fixtures, and focused known-wrong
fixtures. The catalog now contains 128 problems (`F01`-`F68`, `A01`-`A60`).

## COMPLETED: TASK.CURRICULUM.022 - Complete Persistent And Specialized Structures

Added `A61` Persistent Array, `A62` Persistent Trie, `A63` Li Chao Tree, and
`A64` Dynamic Interval Tree with fixed tests, differential generators, and
focused fixtures. Targeted tests passed 28/28; full regression passed 741/741
in about 476 seconds. The 132-problem core curriculum is complete.

## COMPLETED: TASK.PROGRESS.001 - Add Atomic Local Progress Persistence

Accepted ADR-0002 and added a schema-1 `.judge/progress.json` store behind
`ProgressRepository`. `algo test` records attempts, accepted attempts, best AC
test time, and latest verdict; `algo progress [problem-id]` and
`npm run progress -- [problem-id]` provide readback. Writes use a sibling
temporary file plus atomic replacement, while `--no-progress` keeps automation
side-effect free and stress never records progress.

Added strict schema validation, repository round-trip/best-time tests, and an
isolated CLI integration test covering AC, WA, opt-out, JSON version, and output
format. Documentation, security, and Node checks passed; full CTest regression
passed 743/743 in about 521 seconds.

## COMPLETED: TASK.REVIEW.001 - Add Spaced Review Scheduling

Accepted ADR-0003 and upgraded local progress to schema 2 with UTC epoch-day
review fields. Non-AC attempts reset the streak and schedule the next day; the
first AC schedules seven days later, and subsequent ACs schedule 30 days later.
Schema 1 remains readable and migrates on the next recorded attempt.

Added review dates to `algo progress`, a due-date-ordered `algo review` command,
and the matching `npm run review` entry point. Repository tests pin all three
intervals and migration behavior; the isolated CLI flow covers future and due
queues. Documentation and Node checks passed, and the full CTest regression
passed 743/743 in 171.75 seconds with four workers.

## COMPLETED: TASK.WEB.001 - Build The Local Read-Only Practice Workspace

Accepted ADR-0004 and added a dependency-free Node.js loopback adapter plus an
original dense split-pane Web workspace inspired by programming practice sites.
The UI reads the complete 132-problem catalog, Markdown statements, schema-1/2
progress, due reviews, and stores per-problem C++ drafts only in browser storage.
No HTTP route writes repository data or launches learner code.

Added stage, difficulty, text, and review filters; problem metadata and progress
signals; responsive problem and draft drawers; explicit CSP and static asset
allowlisting; and `npm run web`. Fifteen Node tests passed, documentation and
security guards passed, and browser checks covered real-data navigation,
desktop/mobile layout, accessibility labels, and a clean console.
