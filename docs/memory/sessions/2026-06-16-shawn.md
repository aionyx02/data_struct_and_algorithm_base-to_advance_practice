---
type: session_log
status: active
priority: p2
updated: 2026-06-16
context_policy: historical
owner: shawn
---

# Session Log: 2026-06-16 - Shawn

## Summary

- Continued `TASK.CURRICULUM.016` for the Stage A2 Segment Tree family.
- Added the isolated lazy propagation batch:
  - `A13-segment-tree-lazy-range-add`
  - `A14-segment-tree-lazy-range-assign`
- Kept mixed range-add plus range-assign tag composition deferred to `A15`.
- Clarified in the README where learners should place and run their practice
  submissions.

## Actions Taken

- Added deterministic public, boundary, lazy-push invariant, and hidden-sequence
  fixtures for both lazy Segment Tree problems.
- Added correct reference submissions for isolated range-add and range-assign
  lazy propagation over sum aggregates.
- Added targeted wrong submissions that store pending lazy tags but fail to
  push them before partial child queries or updates.
- Extended the Segment Tree generator registry with brute-force differential
  oracles for `A13` and `A14`.
- Registered catalog, deterministic Judge, and seeded stress CTest coverage for
  both new problems.
- Fixed several deterministic fixture operation-count headers before the
  focused suite passed.

## Commands / Validation

```powershell
cmake --preset dev
cmake --build --preset dev
ctest --preset dev -R "(a13|a14|segment_tree_lazy)" --output-on-failure
ctest --preset dev --output-on-failure
.\build\mingw-debug\algo.exe stress A13-segment-tree-lazy-range-add tests\fixtures\submissions\segment_tree_lazy_add_correct.cpp --seed 20260614 --cases 1000
.\build\mingw-debug\algo.exe stress A14-segment-tree-lazy-range-assign tests\fixtures\submissions\segment_tree_lazy_assign_correct.cpp --seed 20260614 --cases 1000
```

Result: full suite green at 399/399 (was 389; +2 catalog, +4 Judge,
+4 stress). `A13` and `A14` correct submissions each passed 1,000 seeded cases
(200,000 operations) at seed `20260614`; both no-push lazy wrong submissions
failed the first generated case with `WA`.

## Stage A2 Segment Tree Mixed Lazy Batch

- Added `A15-segment-tree-lazy-mixed-tags` for a single Segment Tree that
  supports `range_add`, `range_assign`, point `add`/`set`, and sum queries.
- Added fixtures that distinguish assignment-over-add from add-over-assignment
  composition, including a direct partial query after a full-cover assignment
  followed by a full-cover add.
- Added a correct reference submission and a targeted wrong submission that
  stores a later add separately from a pending assignment and then pushes the
  add before the assignment, causing the add to be overwritten.
- Extended the Segment Tree differential generator and registered catalog,
  deterministic Judge, and stress CTest coverage.

### Commands / Validation

```powershell
cmake --preset dev
cmake --build --preset dev
ctest --preset dev -R "(a15|lazy_mixed)" --output-on-failure
ctest --preset dev --output-on-failure
.\build\mingw-debug\algo.exe stress A15-segment-tree-lazy-mixed-tags tests\fixtures\submissions\segment_tree_lazy_mixed_correct.cpp --seed 20260614 --cases 1000
.\build\mingw-debug\algo.exe stress A15-segment-tree-lazy-mixed-tags tests\fixtures\submissions\segment_tree_lazy_mixed_reversed_order.cpp --seed 20260614 --cases 1000
```

Result: full suite green at 404/404 (was 399; +1 catalog, +2 Judge,
+2 stress). The A15 correct submission passed 1,000 seeded cases
(200,000 operations) at seed `20260614`; the reversed-order mixed lazy
submission failed the first generated case with `WA`.

## Stage A2 Segment Tree Positional Search Batch

- Added `A16-segment-tree-positional-search` for point updates, range maximum
  queries, and aggregate-pruned searches.
- Search commands:
  - `first_at_least left threshold`
  - `last_at_least right threshold`
- Added deterministic fixtures for public behavior, singleton boundaries,
  search-boundary invariants, and hidden update/search sequences.
- Added a correct max Segment Tree reference submission and a targeted wrong
  submission that searches the whole tree while ignoring the command's
  left/right boundary.
- Extended the Segment Tree differential generator and registered catalog,
  deterministic Judge, and stress CTest coverage.
- Fixed one fixture authoring slip: `03-search-boundary-invariant.in` declared
  11 operations but listed 12.

### Commands / Validation

```powershell
cmake --preset dev
cmake --build --preset dev
ctest --preset dev -R "(a16|positional_search)" --output-on-failure
ctest --preset dev --output-on-failure
.\build\mingw-debug\algo.exe stress A16-segment-tree-positional-search tests\fixtures\submissions\segment_tree_positional_search_correct.cpp --seed 20260614 --cases 1000
.\build\mingw-debug\algo.exe stress A16-segment-tree-positional-search tests\fixtures\submissions\segment_tree_positional_search_ignores_bounds.cpp --seed 20260614 --cases 1000
```

Result: full suite green at 409/409 (was 404; +1 catalog, +2 Judge,
+2 stress). The A16 correct submission passed 1,000 seeded cases
(200,000 operations) at seed `20260614`; the ignores-bounds positional search
submission failed the first generated case with `WA`.

## Stage A2 Segment Tree Dynamic Sparse Batch

- Added `A17-segment-tree-dynamic-sparse` for point updates and range sums over
  a large `[0, U)` coordinate universe without allocating a full tree.
- Added observable allocation rules through `nodes`: root starts allocated,
  updates allocate only the descended child path, queries allocate nothing, and
  zero no-op updates do not allocate.
- Added deterministic fixtures for public behavior, singleton boundaries,
  sparse allocation invariants, and hidden sparse-coordinate sequences.
- Added a correct dynamic-node reference submission and a targeted wrong
  submission that eagerly allocates both children on every update descent.
- Extended the Segment Tree differential generator and registered catalog,
  deterministic Judge, and stress CTest coverage.
- Fixed one expected-output authoring slip: `03-sparse-allocation-invariant.out`
  initially omitted the `prefix 9` output line.

### Commands / Validation

```powershell
cmake --preset dev
cmake --build --preset dev
ctest --preset dev -R "(a17|dynamic_sparse)" --output-on-failure
ctest --preset dev --output-on-failure
.\build\mingw-debug\algo.exe stress A17-segment-tree-dynamic-sparse tests\fixtures\submissions\segment_tree_dynamic_sparse_correct.cpp --seed 20260614 --cases 1000
.\build\mingw-debug\algo.exe stress A17-segment-tree-dynamic-sparse tests\fixtures\submissions\segment_tree_dynamic_sparse_eager_children.cpp --seed 20260614 --cases 1000
```

Result: full suite green at 414/414 (was 409; +1 catalog, +2 Judge,
+2 stress). The A17 correct submission passed 1,000 seeded cases
(200,000 operations) at seed `20260614`; the eager-children dynamic tree
submission failed the first generated case with `WA`.

## Stage A2 Segment Tree Persistent Versions Batch

- Added `A18-segment-tree-persistent-versions` for versioned point `set`/`add`
  updates and range-sum queries.
- Version `0` is the initial array; every successful update prints and creates
  a new version ID while old versions remain queryable.
- Added deterministic fixtures for public behavior, singleton boundaries,
  version invariants, and hidden branching-version sequences.
- Added a correct path-copying node-pool reference submission and a targeted
  wrong submission that mutates the base version in place before recording a
  new version root.
- Extended the Segment Tree differential generator and registered catalog,
  deterministic Judge, and stress CTest coverage.
- Fixed one fixture authoring slip: `03-version-invariant.in` declared 15
  operations but listed 16.

### Commands / Validation

```powershell
cmake --preset dev
cmake --build --preset dev
ctest --preset dev -R "(a18|persistent_versions)" --output-on-failure
ctest --preset dev --output-on-failure
.\build\mingw-debug\algo.exe stress A18-segment-tree-persistent-versions tests\fixtures\submissions\segment_tree_persistent_versions_correct.cpp --seed 20260614 --cases 1000
.\build\mingw-debug\algo.exe stress A18-segment-tree-persistent-versions tests\fixtures\submissions\segment_tree_persistent_versions_in_place.cpp --seed 20260614 --cases 1000
```

Result: full suite green at 419/419 (was 414; +1 catalog, +2 Judge,
+2 stress). The A18 correct submission passed 1,000 seeded cases
(200,000 operations) at seed `20260614`; the in-place persistent tree
submission failed the first generated case with `WA`.

## Documentation Inventory Audit

- Audited tracked document-like assets and found no tracked `.docx` or `.doc`
  files.
- Confirmed the PDF/PPTX/archive teaching materials under the source material
  folders are Git LFS pointers, not tiny broken files.
- Kept the binary teaching materials because README, project scope, and
  roadmaps identify them as source/reference assets rather than active docs.
- Added a document value standard to `docs/index.md` so future cleanup can
  remove or merge files that lack routing value, duplicate another source, or
  act as vague placeholders.
- Clarified in `docs/project.md` that source course materials are useful for
  curriculum planning but are not documentation entry points for normal agent
  startup.

## Stage A2 Segment Tree Order-Statistic Batch

- Added `A19-segment-tree-order-statistic` (D4) as the eleventh Stage A2
  problem: a count Segment Tree over the value universe `[0, U)` supporting
  `insert`/`erase`/`count`/`less`/`kth`/`size`. The teaching point distinct from
  the A05 Fenwick frequency table is the top-down k-th descend (enter the left
  child when its count `>= k`, else go right after subtracting the left count).
- Created `problem.json`, `statement.md`, four fixed test pairs (`01-public`,
  `02-boundary`, `03-kth-descend-invariant`, `04-hidden-sequence`), one correct
  fixture, and two known-wrong fixtures: `..._kth_wrong_branch.cpp` (right
  descend without subtracting the left count) and `..._less_inclusive.cpp`
  (`<=` rank instead of `<`).
- Added `generateSegmentTreeOrderStatistic` (oracle = plain count array) with a
  fixed invariant prefix (insert 0, 2, 3 then `kth 2` and `less 2`) so both
  wrong fixtures fail on the first generated case regardless of seed; mirrors the
  A05 Fenwick generator. Registered it in `registerSegmentTreeGenerators` and
  wired catalog, Judge (accept + two reject), and stress CTest entries.

### Commands / Validation

```bash
cmake --preset dev
cmake --build --preset dev
build/mingw-debug/algo.exe test A19-segment-tree-order-statistic tests/fixtures/submissions/segment_tree_order_statistic_correct.cpp
build/mingw-debug/algo.exe test A19-segment-tree-order-statistic tests/fixtures/submissions/segment_tree_order_statistic_kth_wrong_branch.cpp
build/mingw-debug/algo.exe test A19-segment-tree-order-statistic tests/fixtures/submissions/segment_tree_order_statistic_less_inclusive.cpp
build/mingw-debug/algo.exe stress A19-segment-tree-order-statistic tests/fixtures/submissions/segment_tree_order_statistic_correct.cpp --seed 20260614 --cases 100
```

Result: build clean. Correct fixture is `AC` on all four fixed tests and on 100
seeded stress cases (20,000 operations) at seed `20260614`. `kth_wrong_branch`
returns `7` where `5` is expected and `less_inclusive` returns `4` where `2` is
expected; both are `WA` on `01-public` and on the first generated stress case
(case seed `10012977635534079361`).

Note: the full `ctest --preset dev` was not run to completion this session — the
machine was overloaded and unrelated catalog tests (`catalog_lists_f43`-`f48`)
were timing out (one took 2016 s), not failing on logic. A19's catalog/Judge/
stress paths were verified directly with `algo`, which is exactly what those
CTest cases drive. Re-run the full suite when the machine is idle (expected
426 tests: previous 419 + 1 catalog, +3 Judge, +3 stress for A19).

## Next Step

- Add `A20` Segment Tree Beats (D5 unlock) to close the twelve-problem Stage A2
  family and finish `TASK.CURRICULUM.016`.
