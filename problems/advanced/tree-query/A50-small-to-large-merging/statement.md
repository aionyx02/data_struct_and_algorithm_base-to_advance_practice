# Small-to-Large Container Merging

Each tree node has an integer color. Input is `n q`, the n colors, then edges. For the rooted subtree of v answer `distinct v`, `count v color`, and `mode v` (most frequent color, smaller color on ties). Build subtree containers by always merging the smaller container into the larger one.
