# DSU On Tree

Each node has an integer color. For every rooted subtree support `distinct v`, `max_frequency v`, and `dominant_sum v`, the sum of all color values tied for maximum frequency. Input is `n q`, colors, then edges and queries. Use the keep-heavy-child DSU-on-tree technique and include v itself in its subtree.
