# HLD With Segment Tree

Maintain node values on a rooted tree. Input is `n q`, one line of n initial values, then edges. Support inclusive `path_sum a b`, `path_add a b delta`, `subtree_sum v`, `point_set v value`, and `value v`. Implement HLD plus a lazy Segment Tree so path and subtree operations are logarithmic in the number of chains.
