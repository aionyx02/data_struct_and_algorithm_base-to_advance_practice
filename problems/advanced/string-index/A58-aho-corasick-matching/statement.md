# Aho-Corasick Matching

Input starts with `patternCount queryCount`, then lowercase patterns. Each query is `count text` for all overlapping occurrences or `distinct text` for the number of patterns appearing at least once. Build failure and output links so suffix patterns and overlaps are counted.
