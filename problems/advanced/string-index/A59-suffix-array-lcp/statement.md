# Suffix Array And LCP Index

Input is `text queryCount`. Use zero-based indices. Support `suffix_array`, `lcp_array` (adjacent suffixes in SA order), `rank i`, `lcp i j`, and lexicographic `compare l1 r1 l2 r2` returning `<`, `>`, or `=`. Build SA and LCP structures rather than sorting per query.
