# Compressed Radix Tree Dictionary

Maintain unique lowercase words in a compressed Radix Tree. Support `insert`, `erase`, `contains`, `prefix_count`, `longest_prefix text`, `words`, and `size`. Duplicate insertion prints `DUPLICATE`; longest prefix prints the longest stored word prefix or `NONE`. Split compressed edges correctly on partial matches.
