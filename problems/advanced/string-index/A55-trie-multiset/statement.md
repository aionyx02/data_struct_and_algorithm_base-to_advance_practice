# Trie Multiset

Maintain lowercase words in a hand-written Trie with terminal and subtree multiplicities. Support `insert word`, `erase word`, `contains word`, `count word`, `prefix_count prefix`, `autocomplete prefix`, and `size`. Erase removes one copy; autocomplete prints unique sorted words or `EMPTY`.
