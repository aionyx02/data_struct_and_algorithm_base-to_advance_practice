# Suffix Automaton Index

Maintain a text under `append character`. Support `contains pattern`, overlapping `occurrences pattern`, `distinct_substrings`, and `longest_repeated`. Use a Suffix Automaton with occurrence propagation; distinct substrings exclude the empty string.
