# Binary XOR Trie Multiset

Maintain unsigned 31-bit integers with multiplicity in a binary Trie. Support `insert x`, `erase_one x`, `count x`, `max_xor x`, `min_xor x`, and `size`. XOR queries print the best XOR value, not the matching key; an empty Trie prints `EMPTY`.
